@model IEnumerable<EditorialViewModel>


<div class="container">
    <h2 class="text-center py-4 title">Editorials</h2>
    @if (Model.Any())
    {
        <table class="table table-striped table-responsive-sm mb-3" width="100%">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Headquarter)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Headquarter)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h4 class="text-center">Data not found</h4>
    }
</div>

@{ await Html.RenderPartialAsync("_FooterPartial"); }